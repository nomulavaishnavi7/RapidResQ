name: rapidresq_patient
description: RapidResQ Patient Emergency App
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  provider: ^6.0.5
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  flutter_map_location_marker: ^8.1.0

  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  firebase_auth: ^4.16.0
  geolocator: ^11.1.0
  geocoding: ^2.1.1
  speech_to_text: ^7.0.3
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true